# 🚀 Инструкция: Парсинг через браузер

## 📋 Обзор

Этот метод использует расширение для браузера, которое работает с вашей активной сессией браузера (уже прошедшей Cloudflare). Это самый надежный способ обойти защиту Cloudflare.

---

## 🔧 Шаг 1: Установка расширения

### Для Chrome/Edge:

1. Откройте браузер Chrome или Edge
2. Перейдите на страницу расширений:
   - Chrome: `chrome://extensions/`
   - Edge: `edge://extensions/`
3. Включите **"Режим разработчика"** (Developer mode) в правом верхнем углу
4. Нажмите **"Загрузить распакованное расширение"** (Load unpacked)
5. Выберите папку: `browser_extension` из проекта Steam Parser

### Для Firefox:

1. Откройте Firefox
2. Перейдите на `about:debugging`
3. Нажмите **"Этот Firefox"** (This Firefox)
4. Нажмите **"Загрузить временное дополнение"** (Load Temporary Add-on)
5. Выберите файл `manifest.json` из папки `browser_extension`

---

## 🎯 Шаг 2: Первый запуск и прохождение Cloudflare

1. Откройте новую вкладку в браузере
2. Перейдите на `https://steamdb.info`
3. Если появится проверка Cloudflare — **пройдите её вручную** (обычно это просто галочка "Я не робот")
4. Дождитесь полной загрузки страницы SteamDB
5. **Важно:** Не закрывайте эту вкладку — она должна оставаться открытой для поддержания сессии

---

## 📊 Шаг 3: Запуск парсинга

### Вариант A: Через Popup расширения (рекомендуется)

1. Нажмите на иконку расширения в панели инструментов браузера
2. В поле "APP IDs" введите список APP IDs:
   - Можно ввести вручную через запятую: `570,730,578080`
   - Или нажать **"Load from app_ids.txt"** и выбрать файл `app_ids.txt` из проекта
3. Нажмите кнопку **"Parse Current Page"**
4. Расширение автоматически:
   - Откроет страницы Compare для батчей по 10 APP IDs
   - Соберет данные через API
   - Сохранит данные в локальное хранилище браузера

### Вариант B: Автоматический парсинг при навигации

Расширение автоматически парсит данные, когда вы открываете:
- Страницы Compare: `https://steamdb.info/charts/?compare=570,730,578080`
- Страницы отдельных игр: `https://steamdb.info/app/570/`

---

## 💾 Шаг 4: Экспорт данных

1. После завершения парсинга нажмите на иконку расширения
2. Нажмите кнопку **"Export Data"**
3. Файл JSON будет автоматически скачан с именем `steamdb_data_YYYY-MM-DD.json`

---

## 🔄 Шаг 5: Импорт данных в основную базу данных

После экспорта данных из расширения, используйте скрипт для импорта в основную базу:

```bash
cd "/Users/mikitavalkunovich/Desktop/Cursor/Steam Parser"
source venv/bin/activate
python import_from_extension.py steamdb_data_2025-12-02.json
```

---

## ⚙️ Настройки

### Размер батча

По умолчанию расширение обрабатывает по 10 APP IDs за раз. Это можно изменить в коде `content.js`:

```javascript
batchSize: 10  // Измените на нужное значение (рекомендуется 10-20)
```

### Задержки

Расширение автоматически делает паузы между запросами:
- 5 секунд после загрузки страницы Compare
- 500 мс между запросами API для каждого APP ID
- 2 секунды между батчами

---

## 📈 Мониторинг прогресса

1. Откройте консоль разработчика (F12)
2. Перейдите на вкладку "Console"
3. Вы увидите логи:
   - `🚀 Starting batch parsing: X batches, Y total APP IDs`
   - `✅ Fetched data for APP ID X: Y points`
   - `✅ All batches completed!`

---

## ⚠️ Важные замечания

1. **Не закрывайте вкладку SteamDB** — она поддерживает сессию
2. **Не закрывайте браузер** во время парсинга
3. **Если Cloudflare снова запросит проверку** — пройдите её вручную
4. **Большие списки (100,000+ APP IDs)** лучше обрабатывать частями:
   - Разбейте `app_ids.txt` на файлы по 10,000 APP IDs
   - Обрабатывайте по одному файлу за раз

---

## 🐛 Решение проблем

### Расширение не работает
- Проверьте, что режим разработчика включен
- Перезагрузите расширение (кнопка "Обновить" на странице расширений)

### Cloudflare блокирует запросы
- Убедитесь, что вы прошли проверку вручную на главной странице SteamDB
- Попробуйте подождать несколько минут и повторить

### Данные не сохраняются
- Проверьте консоль браузера на наличие ошибок
- Убедитесь, что вы находитесь на странице `steamdb.info`

### Медленная работа
- Уменьшите размер батча (например, до 5 APP IDs)
- Увеличьте задержки между запросами

---

## 📝 Формат экспортированных данных

Экспортированный JSON файл имеет структуру:

```json
{
  "570": [
    ["2024-01-01T00:00:00Z", 123456],
    ["2024-01-02T00:00:00Z", 125000],
    ...
  ],
  "730": [...],
  ...
}
```

Где ключ — это APP ID, а значение — массив точек данных `[datetime, players]`.

---

## 🔗 Интеграция с основным парсером

После экспорта данных используйте скрипт `import_from_extension.py` для импорта в основную базу данных SQLite. Это позволит объединить данные, собранные через расширение, с данными, собранными через основной парсер.



