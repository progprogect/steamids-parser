# Упрощенный парсинг - только средние значения

## ✅ Изменения внесены

### Что изменилось:

1. **Парсер упрощен:**
   - Собирает только средние значения (avg) из SteamCharts API
   - Убран парсинг HTML для peak значений
   - Быстрее и проще

2. **Структура данных в БД:**
   - `app_id` (ID) - идентификатор игры
   - `datetime` - дата и время в формате `YYYY-MM-DD HH:MM:SS`
   - `players` - среднее количество игроков
   - `value_type` - всегда 'avg' (для совместимости со старой структурой)

3. **Формат экспорта CSV:**
   ```
   ID,datetime,players
   730,2024-01-01 12:00:00,1000000
   ```

### Гранулярность данных:

Данные сохраняются с той гранулярностью, которую предоставляет API:
- Почасовые данные (если доступны)
- Дневные данные (если доступны)
- Месячные данные (для старой истории)

**Без агрегации** - сохраняются все доступные точки данных.

## 🚀 Продолжение парсинга

Парсер готов к продолжению. При запуске:
- Продолжит с места остановки (102,289 оставшихся APP IDs)
- Будет собирать только средние значения
- Данные будут сохраняться в правильном формате

### Запуск:

```bash
curl -X POST https://worker-production-19aa.up.railway.app/start \
  -F "file=@app_ids.txt"
```

### Проверка статуса:

```bash
curl https://worker-production-19aa.up.railway.app/status | python3 -m json.tool
```

### Экспорт данных:

После завершения парсинга данные можно экспортировать в CSV:

```bash
curl -O https://worker-production-19aa.up.railway.app/export?type=ccu
```

Формат CSV: `ID,datetime,players`


