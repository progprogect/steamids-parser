# SteamDB Parser

Парсер для сбора исторических данных CCU и Price History с SteamDB для ~100,000 игр.

## 🚀 Возможности

- **Высокая скорость:** 225,000 - 450,000 APP IDs/час
- **Параллельная обработка:** до 10 параллельных потоков
- **Compare инструмент:** батчинг по 10-20 APP IDs за запрос
- **Checkpoint механизм:** возможность прервать и возобновить парсинг
- **Прогресс в реальном времени:** статистика и скорость обработки
- **Обработка ошибок:** детальное логирование для повторной обработки

## 📋 Требования

- Python 3.8+
- Playwright (браузеры будут установлены автоматически)

## 🔧 Установка

1. Установите зависимости:
```bash
pip install -r requirements.txt
```

2. Установите браузеры для Playwright:
```bash
playwright install chromium
```

3. Подготовьте файл с APP IDs:
   - Создайте файл `app_ids.txt` в корне проекта
   - Добавьте APP IDs по одному на строку:
   ```
   730
   440
   570
   ```

## 🎯 Использование

Запустите парсер:
```bash
python parser.py
```

Парсер автоматически:
- Загрузит APP IDs из `app_ids.txt`
- Создаст базу данных `data/steam_data.db`
- Начнет парсинг с использованием параллелизма и Compare инструмента
- Будет отображать прогресс в реальном времени
- Сохранит checkpoint для возможности возобновления

## 📊 Результаты

Данные сохраняются в SQLite базу данных `data/steam_data.db`:

### Таблица `ccu_history`
- `app_id` - APP ID игры
- `datetime` - Дата и время (YYYY-MM-DD HH:MM:SS)
- `players` - Количество игроков

### Таблица `price_history`
- `app_id` - APP ID игры
- `datetime` - Дата и время (YYYY-MM-DD HH:MM:SS)
- `price_final` - Цена
- `currency_symbol` - Символ валюты ($)
- `currency_name` - Название валюты (U.S. Dollar)

### Таблица `app_status`
Отслеживание статуса обработки каждого APP ID

### Таблица `errors`
Детальное логирование ошибок для повторной обработки

## ⚙️ Конфигурация

Настройки можно изменить в `config.py`:

- `PARALLEL_THREADS` - количество параллельных потоков (по умолчанию: 10)
- `COMPARE_BATCH_SIZE` - размер батча для Compare (по умолчанию: 10)
- `DELAY_BETWEEN_REQUESTS` - задержка между запросами в секундах (по умолчанию: 1.0)

## 📝 Логи

Логи сохраняются в `logs/parser.log` и выводятся в консоль.

## 🔄 Возобновление парсинга

Парсер автоматически отслеживает прогресс. При повторном запуске:
- Пропустит уже обработанные APP IDs
- Продолжит с места остановки
- Обработает APP IDs с ошибками

## ⚠️ Важные замечания

1. **Cloudflare:** При первом запуске может потребоваться прохождение Cloudflare challenge вручную (если headless=False)
2. **Cookies:** Cookies сохраняются автоматически для минимизации повторных challenge
3. **Rate Limiting:** Используются задержки между запросами для избежания блокировок
4. **Price History:** Парсинг Price History требует дополнительной реализации на основе структуры страницы

## 📈 Производительность

- **Ожидаемая скорость:** 225,000 - 450,000 APP IDs/час
- **Время на 100,000 APP IDs:** 12-24 минуты
- **Ускорение:** 5,625 - 11,250x по сравнению с базовым подходом

## 🐛 Обработка ошибок

Все ошибки логируются в таблицу `errors` и могут быть обработаны отдельно. Статусы ошибок:
- `ccu_error` - ошибка при парсинге CCU
- `price_error` - ошибка при парсинге Price
- `both_error` - ошибки в обоих типах данных

## 📄 Лицензия

Проект для внутреннего использования.




