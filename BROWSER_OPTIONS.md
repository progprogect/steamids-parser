# 🌐 Варианты браузеров для парсинга

Вы можете использовать несколько вариантов браузеров для парсинга SteamDB:

---

## 📌 Вариант 1: Microsoft Edge (Самый простой)

**Преимущества:**
- ✅ Тот же формат расширения, что и Chrome
- ✅ Работает "из коробки" без изменений
- ✅ Хорошо обходит Cloudflare

**Установка расширения:**
1. Откройте `edge://extensions/`
2. Включите "Режим разработчика"
3. Нажмите "Загрузить распакованное расширение"
4. Выберите папку `browser_extension`

**Использование:** Точно так же, как с Chrome (см. `BROWSER_PARSING_QUICKSTART.md`)

---

## 🦊 Вариант 2: Firefox

**Преимущества:**
- ✅ Отличная приватность
- ✅ Хорошо обходит Cloudflare
- ✅ Поддержка расширений

**Установка расширения:**
1. Откройте `about:debugging`
2. Нажмите "Этот Firefox"
3. Нажмите "Загрузить временное дополнение"
4. Выберите файл `browser_extension/manifest_firefox.json`

**Важно:** Для Firefox нужно использовать специальные файлы:
- `manifest_firefox.json` вместо `manifest.json`
- `background_firefox.js` вместо `background.js`
- `content_firefox.js` вместо `content.js`
- `popup_firefox.js` вместо `popup.js`

**Или переименуйте файлы:**
```bash
cd browser_extension
cp manifest_firefox.json manifest.json
cp background_firefox.js background.js
cp content_firefox.js content.js
cp popup_firefox.js popup.js
```

---

## 🍎 Вариант 3: Safari

**Преимущества:**
- ✅ Нативный браузер macOS
- ✅ Отличная производительность

**Ограничения:**
- ⚠️ Расширения для Safari требуют Xcode и подписи
- ⚠️ Более сложная установка

**Альтернатива:** Используйте основной парсер с WebKit через Playwright (см. ниже)

---

## 🤖 Вариант 4: Основной парсер с Firefox/WebKit через Playwright

Вместо расширения браузера можно использовать основной парсер (`parser.py`) с Firefox или Safari через Playwright.

### Использование Firefox:

1. **Установите Firefox для Playwright:**
```bash
playwright install firefox
```

2. **Измените настройки в `config.py`:**
```python
BROWSER_TYPE = "firefox"  # вместо "chromium"
HEADLESS = False  # для прохождения Cloudflare вручную
```

3. **Запустите парсер:**
```bash
python parser.py
```

### Использование Safari (WebKit):

1. **Установите WebKit для Playwright:**
```bash
playwright install webkit
```

2. **Измените настройки в `config.py`:**
```python
BROWSER_TYPE = "webkit"  # вместо "chromium"
HEADLESS = False  # для прохождения Cloudflare вручную
```

3. **Запустите парсер:**
```bash
python parser.py
```

---

## 📊 Сравнение вариантов

| Браузер | Расширение | Playwright | Cloudflare | Сложность |
|---------|-----------|------------|------------|-----------|
| **Chrome** | ✅ | ✅ | ✅✅✅ | Легко |
| **Edge** | ✅ | ✅ | ✅✅✅ | Легко |
| **Firefox** | ✅ | ✅ | ✅✅✅ | Средне |
| **Safari** | ⚠️ | ✅ | ✅✅ | Сложно |

---

## 🎯 Рекомендации

### Для расширения браузера:
1. **Edge** — самый простой вариант (тот же формат, что Chrome)
2. **Firefox** — хорошая альтернатива с лучшей приватностью

### Для основного парсера:
1. **Firefox через Playwright** — если хотите автоматизировать без расширения
2. **WebKit через Playwright** — если используете macOS и хотите Safari

---

## ⚙️ Настройка основного парсера

Откройте `config.py` и измените:

```python
# Выберите браузер
BROWSER_TYPE = "firefox"  # или "webkit", "chromium"

# Для прохождения Cloudflare вручную
HEADLESS = False

# Для автоматического режима (после сохранения cookies)
HEADLESS = True
```

---

## 🔧 Устранение проблем

### Firefox расширение не работает:
- Убедитесь, что используете правильные файлы (`*_firefox.js`)
- Проверьте консоль браузера (F12) на наличие ошибок

### Playwright не может запустить Firefox/WebKit:
```bash
# Переустановите браузеры
playwright install firefox
playwright install webkit
```

### Cloudflare блокирует:
- Используйте `HEADLESS = False` для первого запуска
- Пройдите проверку вручную
- Cookies сохранятся автоматически

---

## 📝 Примеры использования

### Edge (расширение):
```bash
# 1. Установите расширение через edge://extensions/
# 2. Откройте https://steamdb.info
# 3. Пройдите Cloudflare
# 4. Используйте расширение для парсинга
```

### Firefox (Playwright):
```bash
# 1. Установите Firefox
playwright install firefox

# 2. Измените config.py
BROWSER_TYPE = "firefox"
HEADLESS = False

# 3. Запустите парсер
python parser.py

# 4. Пройдите Cloudflare в открывшемся окне
# 5. Cookies сохранятся автоматически
```

---

**Выберите вариант, который вам удобнее!** 🚀


